<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thirteen.phrak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StartupActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateWorkoutActivity" />
        <activity
            android:name=".ExerciseOne"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_workout"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".ExerciseTwo"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_workout"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".ExerciseThree"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_workout"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".FinishActivity"
            android:label="@string/title_activity_finish_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StartupActivity"></activity>
        <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>